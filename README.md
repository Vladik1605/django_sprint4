# Blogicum — Платформа для публикации статей

Многофункциональный веб-сайт на Django для создания, публикации и обсуждения статей. Поддерживает систему пользователей, категоризацию контента, комментарии и медиа-загрузки.

---

## Требования

- **Python 3.9.x** (рекомендуется)
- `pip` (менеджер пакетов)
- встроенный `venv` (виртуальное окружение)
- ОС: Windows / Linux / macOS

> **Примечание:** Проект совместим с Python 3.9.x. При использовании Python 3.10+ возможны проблемы с версией Pillow.

---

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <ссылка_на_ваш_репозиторий>
cd django_sprint4
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
```

**Linux / macOS:**
```bash
python3 -m venv venv
```

### 3. Активация виртуального окружения

**Windows:**
```bash
venv\Scripts\activate
```

**Linux / macOS:**
```bash
source venv/bin/activate
```

### 4. Обновление pip

```bash
python -m pip install --upgrade pip
```

### 5. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 6. Применение миграций БД

```bash
cd blogicum
python manage.py migrate
```

### 7. Создание суперпользователя (администратора)

```bash
python manage.py createsuperuser
```

### 8. Запуск сервера разработки

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: **http://127.0.0.1:8000/**

Панель администратора: **http://127.0.0.1:8000/admin/**

---

## Функциональность

### 👤 Система пользователей

- **Регистрация и авторизация**
  - Создание новых аккаунтов на странице регистрации
  - Вход в систему под своим пользователем
  - Восстановление пароля по электронной почте

- **Профиль пользователя**
  - Просмотр информации о профиле (`/profile/<username>/`)
  - Редактирование данных профиля (для владельца)
  - Смена пароля (для владельца)
  - Список всех публикаций пользователя
  - Счётчик комментариев для каждой публикации

### Управление публикациями

- **Создание статей** (только для авторизованных)
  - Заголовок и основной текст
  - Дата и время публикации (поддержка отложенных публикаций)
  - Категория и местоположение
  - Загрузка изображения (опционально)
  - Статус публикации

- **Редактирование и удаление**
  - Редактирование доступно только автору
  - Удаление публикации с автоматическим редиректом в профиль
  - Кнопки редактирования в профиле и на странице публикации

- **Видимость контента**
  - Скрытие статей (поле `is_published`)
  - Скрытые статьи видны только автору в его профиле
  - Будущие статьи (с датой публикации в будущем) недоступны для общего просмотра
  - Публикуются только статьи с датой публикации, не превышающей текущее время

- **Категоризация**
  - Возможность просмотра статей по категориям
  - Фильтрация контента

### Система комментариев

- **Добавление комментариев** (только для авторизованных)
  - Комментарии появляются под публикацией
  - Принадлежат определённой статье

- **Управление комментариями**
  - Редактирование доступно автору и администратору
  - Удаление доступно автору и администратору
  - Вложенная система комментариев

### Дополнительные возможности

- **Статические страницы** (в приложении `pages`)
  - О проекте (`/about/`)
  - Правила сайта (`/rules/`)
  
- **Обработка ошибок**
  - Пользовательские страницы ошибок (403, 404, 500)
  - Обработка ошибок CSRF

- **Администраторская панель**
  - Управление пользователями
  - Управление публикациями и комментариями
  - Управление категориями и местоположениями
  - Модерация контента

## Тестирование

Для запуска тестов используйте:

```bash
pytest
```
